name: Deploy Slide Decks to GitHub Pages

# Re-trigger marker: run on 2026-05-23 (previous run flaked on runner availability).
# Workflow nay build cac slide deck trong repo va deploy len GitHub Pages.
# De workflow deploy duoc, vao repository Settings -> Pages -> Source -> GitHub Actions.
#
# Link sau khi deploy:
# - Day1:     https://0121ient.github.io/ml-data-engineering/Day1/
# - Day2:     https://0121ient.github.io/ml-data-engineering/Day2/
# - Day3:     https://0121ient.github.io/ml-data-engineering/Day3/
# - Capstone: https://0121ient.github.io/ml-data-engineering/Capstone/

on:
  push:
    branches:
      - main
    paths:
      - "Day1/**"
      - "Day2/**"
      - "Day3/**"
      - "Capstone/**"
      - ".github/workflows/day1-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  test:
    name: Test slide decks
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        deck:
          - Day1
          - Day2
          - Day3
          - Capstone
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: ${{ matrix.deck }}/package-lock.json

      - name: Install dependencies
        working-directory: ${{ matrix.deck }}
        run: npm ci

      - name: Run tests
        working-directory: ${{ matrix.deck }}
        run: npm test

  deploy:
    name: Build and deploy GitHub Pages
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            Day1/package-lock.json
            Day2/package-lock.json
            Day3/package-lock.json
            Capstone/package-lock.json

      - name: Install Day1 dependencies
        working-directory: Day1
        run: npm ci

      - name: Build Day1
        working-directory: Day1
        run: npm run build
        env:
          GITHUB_PAGES: "true"

      - name: Install Day2 dependencies
        working-directory: Day2
        run: npm ci

      - name: Build Day2
        working-directory: Day2
        run: npm run build
        env:
          GITHUB_PAGES: "true"

      - name: Install Day3 dependencies
        working-directory: Day3
        run: npm ci

      - name: Build Day3
        working-directory: Day3
        run: npm run build
        env:
          GITHUB_PAGES: "true"

      - name: Install Capstone dependencies
        working-directory: Capstone
        run: npm ci

      - name: Build Capstone
        working-directory: Capstone
        run: npm run build
        env:
          GITHUB_PAGES: "true"

      - name: Assemble Pages artifact
        shell: bash
        run: |
          mkdir -p pages/Day1 pages/Day2 pages/Day3 pages/Capstone
          cp -R Day1/dist/. pages/Day1/
          cp -R Day2/dist/. pages/Day2/
          cp -R Day3/dist/. pages/Day3/
          cp -R Capstone/dist/. pages/Capstone/

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pages

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
